services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=spring_tricount'
      - 'MYSQL_ROOT_PASSWORD=root'
      - 'MYSQL_USER=dev'
      - 'MYSQL_PASSWORD=password'
    ports:
      - '3306:3306'
